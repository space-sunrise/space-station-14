<controls:FancyWindow xmlns="https://spacestation14.io"
               xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
               Title="{Loc 'voice-mask-name-change-window'}"
               MinSize="10 30"> <!-- Sunrise Edit -->
    <BoxContainer Orientation="Vertical" Margin="5">
        <Label Text="{Loc 'voice-mask-name-change-info'}" />
        <BoxContainer Orientation="Horizontal" Margin="5">
            <LineEdit Name="NameSelector" HorizontalExpand="True" />
            <Button Name="NameSelectorSet" Text="{Loc 'voice-mask-name-change-set'}" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal" Margin="5">
            <Label Text="{Loc 'voice-mask-name-change-speech-style'}" />
            <OptionButton Name="SpeechVerbSelector" /> <!-- Populated in LoadVerbs -->
        </BoxContainer>
        <!-- Sunrise-Start -->
        <BoxContainer Orientation="Vertical" Visible="True" Name="TTSContainer">
            <Label Text="{Loc 'voice-mask-voice-change-info'}" />
            <OptionButton Name="VoiceOptionButton" /> <!-- Populated in LoadVerbs -->
            <BoxContainer Orientation="Horizontal" Margin="5 10 5 5" HorizontalAlignment="Center">
                <Button Name="VoicePlayButton"
                        Text="{Loc 'borg-voice-window-play-button'}"
                        Margin="0 0 5 0" />
            </BoxContainer>
        </BoxContainer>
        <!-- Sunrise-End -->
    </BoxContainer>
</controls:FancyWindow>
